"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || function (mod) {
    if (mod && mod.__esModule) return mod;
    var result = {};
    if (mod != null) for (var k in mod) if (k !== "default" && Object.prototype.hasOwnProperty.call(mod, k)) __createBinding(result, mod, k);
    __setModuleDefault(result, mod);
    return result;
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.createStdioOptions = createStdioOptions;
exports.startServer = startServer;
exports.createUriConverters = createUriConverters;
/* --------------------------------------------------------------------------------------------
 * Copyright (c) Microsoft Corporation. All rights reserved.
 * Licensed under the MIT License. See License.txt in the project root for license information.
 * ------------------------------------------------------------------------------------------ */
const vscode = __importStar(require("vscode"));
const vscode_languageclient_1 = require("vscode-languageclient");
class ReadableStreamImpl {
    errorEmitter;
    closeEmitter;
    endEmitter;
    readable;
    constructor(readable) {
        this.errorEmitter = new vscode_languageclient_1.Emitter();
        this.closeEmitter = new vscode_languageclient_1.Emitter();
        this.endEmitter = new vscode_languageclient_1.Emitter();
        this.readable = readable;
    }
    get onData() {
        return this.readable.onData;
    }
    get onError() {
        return this.errorEmitter.event;
    }
    fireError(error, message, count) {
        this.errorEmitter.fire([error, message, count]);
    }
    get onClose() {
        return this.closeEmitter.event;
    }
    fireClose() {
        this.closeEmitter.fire(undefined);
    }
    onEnd(listener) {
        return this.endEmitter.event(listener);
    }
    fireEnd() {
        this.endEmitter.fire(undefined);
    }
}
class WritableStreamImpl {
    errorEmitter;
    closeEmitter;
    endEmitter;
    writable;
    constructor(writable) {
        this.errorEmitter = new vscode_languageclient_1.Emitter();
        this.closeEmitter = new vscode_languageclient_1.Emitter();
        this.endEmitter = new vscode_languageclient_1.Emitter();
        this.writable = writable;
    }
    get onError() {
        return this.errorEmitter.event;
    }
    fireError(error, message, count) {
        this.errorEmitter.fire([error, message, count]);
    }
    get onClose() {
        return this.closeEmitter.event;
    }
    fireClose() {
        this.closeEmitter.fire(undefined);
    }
    onEnd(listener) {
        return this.endEmitter.event(listener);
    }
    fireEnd() {
        this.endEmitter.fire(undefined);
    }
    write(data, _encoding) {
        if (typeof data === 'string') {
            return this.writable.write(data, 'utf-8');
        }
        else {
            return this.writable.write(data);
        }
    }
    end() {
    }
}
function createStdioOptions() {
    return {
        in: {
            kind: 'pipeIn',
        },
        out: {
            kind: 'pipeOut'
        },
        err: {
            kind: 'pipeOut'
        }
    };
}
async function startServer(process, readable = process.stdout, writable = process.stdin) {
    if (readable === undefined || writable === undefined) {
        throw new Error('Process created without streams or no streams provided.');
    }
    const reader = new ReadableStreamImpl(readable);
    const writer = new WritableStreamImpl(writable);
    process.run().then((value) => {
        if (value === 0) {
            reader.fireEnd();
        }
        else {
            reader.fireError([new Error(`Process exited with code: ${value}`), undefined, undefined]);
        }
    }, (error) => {
        reader.fireError([error, undefined, undefined]);
    });
    return { reader: new vscode_languageclient_1.ReadableStreamMessageReader(reader), writer: new vscode_languageclient_1.WriteableStreamMessageWriter(writer), detached: false };
}
function createUriConverters() {
    const folders = vscode.workspace.workspaceFolders;
    if (folders === undefined || folders.length === 0) {
        return undefined;
    }
    const c2p = new Map();
    const p2c = new Map();
    if (folders.length === 1) {
        const folder = folders[0];
        c2p.set(folder.uri.toString(), 'file:///workspace');
        p2c.set('file:///workspace', folder.uri.toString());
    }
    else {
        for (const folder of folders) {
            const uri = folder.uri.toString();
            c2p.set(uri, `file:///workspace/${folder.name}`);
            p2c.set(`file:///workspace/${folder.name}`, uri);
        }
    }
    return {
        code2Protocol: (uri) => {
            const str = uri.toString();
            for (const key of c2p.keys()) {
                if (str.startsWith(key)) {
                    return str.replace(key, c2p.get(key) ?? '');
                }
            }
            return str;
        },
        protocol2Code: (value) => {
            for (const key of p2c.keys()) {
                if (value.startsWith(key)) {
                    return vscode.Uri.parse(value.replace(key, p2c.get(key) ?? ''));
                }
            }
            return vscode.Uri.parse(value);
        }
    };
}
